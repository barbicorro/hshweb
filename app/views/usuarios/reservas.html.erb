<div class="container-fluid bg-dark pb-4">



  <div class="text-center offset -2"><h2 class="text-white mb-3">Mis reservas</h2></div>

  <div class="row">
    <table style="table", class="table table-dark">
      <thead>
        <tr>
          <th width "80%"> Residencia </th>
          <th width "100%"> Semana </th>
          <th width "80%"> Acciones </th>
          <th colspan="4"> </th>
        </tr>
      </thead>
      <tbody>
          <tr>
            <td> <%= Residence.find(Week.find(current_user.week1_id).residence_id).title%> </td>
            <td><%= Week.find(curret_user.week1_id).period %> </td>
            <td> Falta el boton </td>
          </tr>

          <tr>
            <td> <%= Residence.find(Week.find(current_user.week2_id).residence_id).title%> </td>
            <td><%= Week.find(current_user.week2_id).period %> </td>
            <td> Falta el boton </td>
          </tr>

        </tbody>
      </table>
    </div>


  <button class="btn btn-block btn-light col-4 offset-4 mt-3"> <%= link_to "Volver", root_path %>
</button>
</div>
